add: (u32, u32) -> u32 = (a, b) -> a;

Vec2 :: (u32, u32);
add_vec2: (Vec2, Vec2) -> Vec2 = (lhs, rhs) -> (lhs.0.add(rhs.0), lhs.1.add(rhs.1));

Option :: [Some u32, None ()];
unwrap: Option -> u32 = x -> match x {
    Some[x] -> x,
    None[] -> unreachable
};
